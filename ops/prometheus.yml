global:
  scrape_interval: 15s

scrape_configs:
  - job_name: nexus
    metrics_path: /metrics
    static_configs:
      # host.docker.internal reaches the uvicorn process on the host machine
      - targets: ["host.docker.internal:8000"]
